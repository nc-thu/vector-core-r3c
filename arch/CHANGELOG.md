# 架构线 CHANGELOG — Architecture & Performance（拍数与调度建模）

管什么：周期模型（r3c_model / pe_sizing / 拍数模型）、编译器调度与布局、读压缩路线——不写 RTL，产出 RTL 需求清单给硬件线。口径提醒：@198.5 MHz 下 M 拍 = M/198.5 秒（旧报告里个别"ms"为单位笔误，以拍数换算为准）。
版本规则与命名见根目录 [LINES.md](../LINES.md)。服务器约定 `/tmp/arch_v{N}/`（历史轮沿用当时目录名）。

---

## v6 —— 2026-09-08 22:08 LUT 锚点修正 + B8 定版收口 + R2 模型修正：**定版 B8-48**（B8-64 翻案关死）【09-09 补一版修正，见下】
- 改动：b8_final.py 三件事——①LUT 面积模型用实测锚点修正（v5 每物理列 3140 LUT 高估 27.6%，改 2461.4 LUT/列 + 25k 基础设施，eng48/eng64 双实测点）；②H2 走读 +1 拍的帧级敏感性（DRAIN 64→65）；③R5/COPY 的 H3 进出清单量化（h3_r5_copy.md）。B8-64 翻案窗口（v5 说 98% 贴线 → 修正后 78.4% 有余量）用 eng64 综合实测关死。
- 关键数字：**定版 B8-48**（eng64 三条件两不过：WNS −0.696 FAIL、功耗 10,709 mW > 9,000 FAIL、LUT 78.4% PASS；补刀——降频 250.4MHz 用 HP64=0.970 s 反而比 B8-48 的 0.909 s 慢，翻案只剩深度重流水一条不可预期的路）；LUT 锚点修正后 eng64 实测 155,629 与外推 157,530 差 1.2%（模型可用）；DRAIN 64→65 全档 ≤+0.27%（GEMM +0.25%、TB +0.0%、HP64 0.909→0.910 s，H2 拍数代价关闭）；**R2 实测修正——模型 −19.1% 是理想上界，4 段实测 −7.4%**（读服务 −8.1%、两口重叠最高 15.5%；差距=单 outstanding 从机串行+真实段形态+无 W 段白付开销；帧级外推明确不做，等 H3 全帧调度重排）；R5 写侧压缩在 B8-48 下收益为零（写腿非瓶颈，不进 H3）、COPY 40.3M+ACTV 10.0M 拍=计算腿 18.3%（全消除理论地板 0.909→0.744 s，先编译器侧逐类分解再动手）。
- 本地：[v6_2026-09-08_2000_h1anchor_r2_b64_final/](v6_2026-09-08_2000_h1anchor_r2_b64_final/)（b8_final.py / b8_final.json / measured.json / h3_r5_copy.md）。
- 复现：`cd arch/v6_2026-09-08_2000_h1anchor_r2_b64_final && python b8_final.py`（实测数在 measured.json，手工维护）。
- 下游：硬件线 H3 B8-48 系统集成（R2 合流+底本修复回移）；COPY 分解交编译器线。

## v5 —— 2026-09-08 14:28 B8 选型定案 + 读压缩三路线重算：B8-48 物理列为甜点（帧时间 −35%、DSP −50%）
- 改动：B8 进 pe_sizing 周期模型当 PE 旋钮（4 MAC/接口拍/DSP、接口时钟 303.2 MHz、逻辑列=2×物理列、PE 169 LUT/颗 cl64 实测锚点），物理列 {24,32,48,64,96} × {R3C, B8} 全扫描 + 读压缩三路线同一张表重算 + 两个敏感性（读出 2× 宽、requant 不加倍 DRAIN 128）。
- 关键数字：**B8-48（逻辑 96 列）HP64 帧时间 0.909 s（−34.5%）、DSP 768 颗（−50%）、LUT 估 76%、每帧拍数与 R3C-96 完全相同只换时钟**；B8-96 光 PE 核 259,584 LUT = 器件 113% **放不下**——B8 第一约束=LUT、第二=功耗，DSP 余量失效；R3C 窄到 48 列以下破 2.13 s 预算，B8-24（22% DSP、4.3 W）仍比基线 −9%，窄阵列第一次划算；B8 利用率 26.5%（R3C 53.0%）= 行组下限 68 拍接口域地板；requant 不加倍 GEMM +16%（帧 +10.8%），读出 2× 宽只 −6~8%；读压缩三路线真机 HP64 无帧时间收益（读侧仅 20.1M 拍，从来不是真机的墙），路线 2 双读引擎仍是 TB 单刀（B8-48 TB 1.125→0.909 s 两口径归一），路线 1 价值重定位为 ctx 字节 −31%（功耗项）；新暴露 **TB 写墙 W=218.4M 拍**（R5 写侧整形 = H3 后半下一张牌）。
- 本地：[v5_2026-09-08_1414_b8_sizing_read_compress/](v5_2026-09-08_1414_b8_sizing_read_compress/)（b8_scan.py / b8_scan.json / rtl_requirements_h3.md / 2026-09-08_1428 HTML）。
- 复现：`cd arch/v5_2026-09-08_1414_b8_sizing_read_compress && python b8_scan.py`（约 2 s）。
- 下游：硬件线 H1 按 rtl_requirements_h3.md 的 B1–B7 做 B8-48 集成门（B8-96 kill）；H3 按新优先级（双读引擎先行、WRAM4 顺手、驻留跟编译器线联动）。

## v4 —— 2026-09-01 11:38 R3C 行组流水模型 + 96 列选型定案：全帧 275.6M 拍 ≈ 1.39 s（HP64）
- 改动：行组周期公式改 max(k+2, 68+wb)（PE 快照的模型化）；pe_sizing 双口径（固定流硬切/重编译）扫描 COLS 92~108，定案 **COLS=96**（1536 DSP=88.9%，重编译口径仅 +5.2% 拍数，利用率反升至 53.0%）。
- 关键数字：GEMM 336.4→185.3M 拍（−44.9%）；TB 332.1M ≈ 1.67 s；HP64 275.6M ≈ 1.39 s；读压缩路线图（双读引擎单刀过 235.6M 线；LOAD_CTX 680 MB 中 81% 是引擎自产图 DDR 往返，驻留候选 191.6 MB）；预算 2.13 s（64 步 × 33 ms @30 Hz）裕量 1.5×。
- 本地：[hb_fpga_impl/19_r3c_arch/](../hb_fpga_impl/)（r3c_model.py / pe_sizing.py / r3c_model.json / pe_sizing.json）、hb_fpga_impl/20_read_compress/。
- 关联：硬件线 v3（R3C RTL）按此模型落地，实测对上。

## v3 —— 2026-09-01 00:49 a3 融合 + R1/R2 收益重算：427.2M 拍，GEMM 占比 78.9%
- 改动：192 站 actv 融合模型（986.7→899.4M，−8.8%）；R1+R2 读写并发建模（TB 从机物理上是 AXI 全双工）；关键判断——R1 单独不够（46.8%），必须 R2 把 LOAD_CTX 也藏进 GEMM。
- 关键数字：计算通道 387.2M > 读 292.1M > 写 218.4M（a3 真实流需求口径）；a4 驻留+残差未降（901M，resid 98% 结构性跳过）——编译器纯调度路线到头。
- 本地：hb_fpga_impl/11_overlap_plan/、12_actv/a3/、13_rtl_plan/。

## v2 —— 2026-08-31 17:4x compute-bound 编译器改造：1216.5→986.4M 拍（−18.9%）
- 改动：只改调度/布局零改数值语义——段界/列块合并（杠杆 A）、多头 A 图单装/V-T 单装/WRAM 驻留表/LOAD_W 贴发射（杠杆 B/C/D）。
- 关键数字：LOAD_CTX 字节 −45.7%（段内重装 −89.8%）；拍数模型（5 系数最小二乘，1468 段拟合）外样偏差 −0.11%；位精确门 15/15 段；**口径翻案**——旧账"GEMM 55.4%"是计数 bug，真实排序 LOAD_CTX 34.3% 第一。
- 本地：hb_fpga_impl/09_cbound/、10_cbound_report/。服务器（数据保留）：/tmp/ae_cb。

## v1 —— 2026-08-31（上午）v0 朴素调度全模型分层测拍与口径锚定
- 关键数字：441 类代表段 Verilator 双模式实测，总计 ref 1114.5M / pf1 1080.8M 拍；MAC 对账 RTL 223.76G = 编译器逐类全等；有效 MAC 占峰值 7.8%（搬运占大头）；挖出 dma_len 编码溢出（1276 段）。锚定口径 1216.5M 拍 ≈ 6.13 s。
- 本地：hb_fpga_impl/05_sim/（TOTAL / MEASURE_NOTES / cycles_by_type）。

## 未占号条目
- 2026-09-09 09:52 **架构分析页：B8-48 定版后的时间账与优化方向**（四腿三口径图/计算腿解剖/负结果清单/杠杆解锁顺序瀑布，5 图）→ [v6_2026-09-08_2000_h1anchor_r2_b64_final/2026-09-09_0952_架构分析_B8-48定版后的时间账与优化方向.html]。**含一处对 v6 h3_r5_copy.md 的修正**："COPY 全消地板 0.744s（−18%）"只算计算腿没套帧模型 max()，写腿 218.4M 先拦住，正确帧级 0.852s（−6.3%）；杠杆解锁顺序＝停顿吸收 −14.5% → COPY（被写墙封顶）→ R5 写压缩（条件启动，② 后再 −15.2%，三牌全兑现 0.611s）→ requant 加倍远期；墙序列 计算 235.5 → 写 218.4 → 读 184.0M（真机无读墙）；R5 从"不进 H3"细化为"H4 条件启动"。
- 2026-09-08 13:12 **600 MHz PE 后路线讨论页**（B8×6.1×/DSP、计算墙拆除、墙全在喂数、硬件/架构分工 H1-H4/A1-A2、决策点 D1-D3）→ round_report_hw_arch_next/2026-09-08_1312_*.html（也归 plans/ 口径）。
- 2026-09-01 11:17 R3 行组流水方案页（方案 C 选型过程，后被 v4 模型吸收）→ hb_fpga_impl/18_r3_plan/。
- 2026-08-31 10:30~11:28 问题讨论页 v1~v3（16× 差距拆解、on-chip 调研、INT4 调研并入；归因图数字已被 v2 口径翻案作废）→ hb_fpga_impl/08_report/。
- 史前：research_evo1_hif8/ 的 v2/v3 建模（Evo-1 时代，HoloBrain 换向后仅作方法论参考）；round_report_hb_prof、round_report_hb_arch（08-30 摸底，feed 墙 99.8%、II=1 串行 0.70 s→预取 0.27 s）。
